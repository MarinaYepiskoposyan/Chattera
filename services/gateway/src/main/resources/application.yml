spring:
  application:
    name: gateway

server:
  # 8080 is reserved for Keycloak in local dev (see docker-compose.yml).
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
